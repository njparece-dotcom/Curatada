# Architecture — Curatada

> This file is the **lean structural reference** for Curatada. The full narrative TDD with reasoning, ADRs, and decision history lives in Confluence: [Curatada — Technical Design Document](https://nextideaup.atlassian.net/wiki/spaces/Curatada/pages/8093697/Curatada+Technical+Design+Document).
>
> **Update policy:** This file is auto-generated by the `nextideaup-tdd` Claude Skill. Do not edit by hand — changes will be overwritten. To update architectural decisions, run the skill in `--update` mode against the live repo.
>
> **Last generated:** 2026-05-12 by `nextideaup-tdd v1.0` (`--init` run).

---

## Stack

| Layer | Tool / Framework |
| --- | --- |
| Language | TypeScript 5 (strict), Node 20 |
| Framework | Next.js 15.5.18 (App Router) + React 19.1 + Tailwind 3 |
| Database | PostgreSQL 16 (Railway plugin in prod; `postgres:16-alpine` in dev) |
| Object storage | Cloudflare R2 (private bucket, presigned URLs) |
| Hosting | Railway (single app service + Postgres plugin) |
| CI / CD | None (push-to-deploy from GitHub; no Actions workflow yet) |
| Tests | None (manual `tsc --noEmit` + `next build` pre-PR) |
| Auth | NextAuth v4 (JWT strategy, Credentials + Google + Azure AD + Apple) |

## Schema

Four parallel "collection module" triplets (parent + images + valuations) plus shared users/auth + pursuit tables. All data tables are `user_id`-scoped.

```
users(id PK, email UNIQUE, name, image, password_hash, email_verified,
      last_login_at, created_at, updated_at)
accounts(id PK, user_id FK→users, type, provider, provider_account_id, UNIQUE(provider, provider_account_id), …)
user_modules(user_id FK, module IN('guitars','watches','automobiles','collectibles'),
             enabled, PK(user_id, module))

-- One triplet per module — only column shape differs:
{module}_items(id PK, user_id FK, category CHECK(...), brand, model, year,
               condition CHECK(...), purchase_price NUMERIC, …,
               created_at, updated_at)
{module}_images(id PK, {module}_id FK→{module}_items,
                filename, path, mime_type, size, is_primary, sort_order, created_at)
{module}_valuations(id PK, {module}_id FK,
                    valuation_type CHECK('ai','user'), price NUMERIC,
                    notes, comparable_sales JSONB, created_at)

-- Pursuits (saved searches) — one table per module, plus shared findings table:
{module}_pursuits(id PK, user_id FK, …, status CHECK('active','found','paused'), …)
pursuit_findings(id PK, pursuit_type CHECK(...), pursuit_id, source, title, url,
                 price, …, first_seen_at, last_seen_at,
                 UNIQUE(pursuit_type, pursuit_id, url))

-- Migration tracking
schema_migrations(filename PK, applied_at)
```

Image FK columns diverge by module (`guitar_item_id`, `watch_item_id`, `auto_id`, `iod_id`) — captured in `CollectionConfig.imageFkColumn`.

**Migrations:** `db/migrations/*.sql` — tracked in `schema_migrations` by filename. Applied transactionally on container boot via `scripts/migrate.js` (Dockerfile CMD: `node scripts/migrate.js && exec node server.js`).

## API

Routes implemented under `app/api/**/route.ts`. Four collection modules use factory-generated handlers (`lib/collection-handler.ts`, `lib/valuation-handler.ts`); everything else is hand-written. 53 route files total.

| Method | Path | Auth | Handler |
| --- | --- | --- | --- |
| ANY | `/api/auth/[...nextauth]` | NextAuth flow | `app/api/auth/[...nextauth]/route.ts` |
| POST | `/api/auth/register` | Open | `app/api/auth/register/route.ts` |
| GET / POST | `/api/{guitars,watches,automobiles,iod}` | Session, `user_id`-scoped | `app/api/{module}/route.ts` (factory re-export) |
| GET / PATCH / DELETE | `/api/{module}/[id]` | Session + ownership | `app/api/{module}/[id]/route.ts` (factory re-export) |
| POST | `/api/{module}/[id]/value` | Session + ownership | `app/api/{module}/[id]/value/route.ts` (factory re-export) |
| GET | `/api/{module}/[id]/valuations` | Session + ownership | `app/api/{module}/[id]/valuations/route.ts` |
| POST | `/api/{module}/import` | Session | `app/api/{module}/import/route.ts` |
| POST | `/api/{module}/value-batch` (guitars + watches) | Session | `app/api/{module}/value-batch/route.ts` |
| GET / POST / DELETE | `/api/{module}/[id]/images*` (autos + iod) | Session + ownership | `app/api/{module}/[id]/images/route.ts` |
| ALL | `/api/{module}-pursuits[*]` | Session, `user_id`-scoped | `app/api/{module}-pursuits/**/route.ts` |
| POST | `/api/pursuits/run-search` | Session OR `Bearer $CRON_SECRET` | `app/api/pursuits/run-search/route.ts` |
| GET | `/api/dashboard`, `/api/dashboard/history` | Session | `app/api/dashboard/{route,history/route}.ts` |
| POST / PUT | `/api/data/export`, `/api/data/import` | Session | `app/api/data/{export,import}/route.ts` |
| POST | `/api/upload` | Session (route-level; middleware-excluded) | `app/api/upload/route.ts` |
| GET | `/api/uploads/[...path]` | Open (returns 302 to 1-hour presigned R2 URL) | `app/api/uploads/[...path]/route.ts` |
| GET / PUT | `/api/user/modules` | Session | `app/api/user/modules/route.ts` |
| GET | `/api/mgmt/v1/{health,summary,users}` | `Bearer $MGMT_API_TOKEN` + 60/min IP rate limit | `app/api/mgmt/v1/*/route.ts` |

## Integrations

| Service | Env var(s) | Source file |
| --- | --- | --- |
| Anthropic API (valuations + pursuit web_search) | `ANTHROPIC_API_KEY` | `lib/valuation-handler.ts`, `lib/pursuit-search.ts` |
| Cloudflare R2 (image object storage) | `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET` | `lib/storage/r2.ts` |
| Google OAuth (optional) | `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` | `lib/auth.ts` |
| Azure AD OAuth (optional) | `AZURE_AD_CLIENT_ID`, `AZURE_AD_CLIENT_SECRET`, `AZURE_AD_TENANT_ID` | `lib/auth.ts` |
| Apple Sign-In (optional) | `APPLE_ID`, `APPLE_TEAM_ID`, `APPLE_PRIVATE_KEY`, `APPLE_KEY_ID` | `lib/auth.ts` |
| Cross-app mgmt dashboard (consumer) | `MGMT_API_TOKEN`, `MGMT_REQUIRE_INTERNAL` | `lib/mgmt/*` |
| Railway Postgres | `DATABASE_URL` (`${{Postgres.DATABASE_URL}}` reference) | `lib/db.ts`, `scripts/migrate.js` |
| Daily pursuit cron (sibling container) | `CRON_SECRET` | `app/api/pursuits/run-search/route.ts`, `docker/cron/entrypoint.sh` |

## Auth

- **Strategy:** NextAuth v4, JWT session (`session.strategy: "jwt"`). No DB session table. Cookie carries signed JWT containing `userId`.
- **Source:** `lib/auth.ts` (provider config + callbacks). Route handlers all call `getServerSession(authOptions)` at the top.
- **`last_login_at`:** Best-effort stamp on every successful Credentials `authorize()` and OAuth `signIn()` (`lib/auth.ts:updateLastLogin`). Used by `/api/mgmt/v1/summary` for "active in last 30 days".
- **Authorization model:** Ownership-based — every collection-data query filters `WHERE user_id = $session.user.id`. No user-level admin role; the mgmt API uses a separate app-wide bearer token.
- **Roles:** Anonymous · Authenticated user · Cron container (`Bearer $CRON_SECRET`) · Mgmt-API caller (`Bearer $MGMT_API_TOKEN`).
- **Middleware:** `middleware.ts` runs `withAuth` on everything except `login`, `register`, `api/auth`, `api/pursuits/run-search`, `api/mgmt`, `api/upload`, `_next/static`, `_next/image`, `favicon.ico`, `uploads`.

## Deployment

- **Production:** `https://curatada-production.up.railway.app` — Railway service auto-built from `main` branch via the repo `Dockerfile` (multi-stage: deps → builder → runner). Container CMD runs migrations then execs the Next server. Postgres via Railway's Postgres plugin.
- **Staging:** none.
- **Local dev:** `docker compose up -d db` + `npm run dev` against `.env.local`.
- **CI:** No GitHub Actions workflow exists. `gh pr merge --auto` is enabled but has no required check, so it merges instantly. (Tracked as known gap in the TDD's Section 9.)
- **Rollback:** Railway → Deployments → previous deploy → "Redeploy". No DB-level rollback; new migrations are forward-only.

## Where to Find Things

| What | Where |
| --- | --- |
| API routes | `app/api/**/route.ts` |
| Business logic | `lib/collection-handler.ts`, `lib/valuation-handler.ts`, `lib/pursuit-search.ts`, `lib/mgmt/data.ts`, `lib/storage/r2.ts` |
| Per-module config + prompts | `lib/collections/*.ts` |
| Database migrations | `db/migrations/*.sql` |
| Auth setup | `lib/auth.ts`, `middleware.ts`, `app/api/auth/[...nextauth]/route.ts`, `app/api/auth/register/route.ts` |
| Form shell (Add/Edit modals) | `lib/hooks/use{ImageUpload,EditImageList}.ts`, `components/forms/*` |
| Tests | (none — `tsc --noEmit` + `next build` is the manual gate) |
| CI config | (none — `gh pr merge --auto` with no required check) |
| Local dev compose | `docker-compose.yml` |
| Container build | `Dockerfile` |
| Cron container | `docker/cron/` |
| Admin scripts | `scripts/migrate.js`, `scripts/set-password.js` |
| Design system | `Looks/DESIGN.md`, `tailwind.config.ts`, `app/globals.css` |
| Developer guide | `CLAUDE.md` |

---

For the **why** behind any of these choices, read the [Confluence TDD](https://nextideaup.atlassian.net/wiki/spaces/Curatada/pages/8093697/Curatada+Technical+Design+Document).
